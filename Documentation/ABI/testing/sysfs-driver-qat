What:		/sys/bus/pci/devices/<BDF>/qat/state
Date:		June 2022
KernelVersion:	6.0
Contact:	qat-linux@intel.com
Description:	(RW) Reports the current state of the QAT device. Write to
		the file to start or stop the device.

		The values are:

		* up: the device is up and running
		* down: the device is down


		It is possible to transition the device from up to down only
		if the device is up and vice versa.

		This attribute is only available for qat_4xxx devices.

What:		/sys/bus/pci/devices/<BDF>/qat/cfg_services
Date:		June 2022
KernelVersion:	6.0
Contact:	qat-linux@intel.com
Description:	(RW) Reports the current configuration of the QAT device.
		Write to the file to change the configured services.

		Please note that the order of services is unrestricted in
		configurations including more than one service (i.e.
		sym;asym is equal to asym;sym).

		The values are:

		* sym;asym: the device is configured for running crypto
		  services
		* dc: the device is configured for running compression service
		* dcc: identical to dc but enables the dc chaining feature,
		  hash then compression. If this is not required chose dc
		* sym: the device is configured for running symmetric crypto
		  service
		* asym: the device is configured for running asymmetric crypto
		  service
		* asym;dc: the device is configured for running asymmetric
		  crypto and compression services
		* sym;dc: the device is configured for running symmetric crypto
		  and compression services

		Additional values available for qat_6xxx devices only:
		* decomp: the device is configured for running decompression
		  service
		* dc;decomp: the device is configured for running compression
		  and decompression services
		* sym;decomp: the device is configured for running symmetric
		  crypto and decompression services
		* asym;decomp: the device is configured for running
		  asymmetric crypto and decompression services
		* sym;asym;dc: the device is configured for running crypto
		  and compression services
		* sym;asym;decomp: the device is configured for running crypto
		  and decompression services
		* sym;dc;decomp: the device is configured for running
		  symmetric crypto, compression and decompression services
		* asym;dc;decomp: the device is configured for running
		  asymmetric crypto, compression services and
		  decompression services

		It is possible to set the configuration only if the device
		is in the `down` state (see /sys/bus/pci/devices/<BDF>/qat/state)

		Crypto kernel instances are created when both sym and asym
		crypto services are enabled.
		Compression service will create a kernel instance when enabled
		as a single service.

		The following example shows how to change the configuration of
		a device configured for running crypto services in order to
		run data compression::

			# cat /sys/bus/pci/devices/<BDF>/qat/state
			up
			# cat /sys/bus/pci/devices/<BDF>/qat/cfg_services
			sym;asym
			# echo down > /sys/bus/pci/devices/<BDF>/qat/state
			# echo dc > /sys/bus/pci/devices/<BDF>/qat/cfg_services
			# echo up > /sys/bus/pci/devices/<BDF>/qat/state
			# cat /sys/bus/pci/devices/<BDF>/qat/cfg_services
			dc

		This attribute is only available for qat_4xxx devices.

What:		/sys/bus/pci/devices/<BDF>/qat/pm_idle_enabled
Date:		June 2023
KernelVersion:	6.5
Contact:	qat-linux@intel.com
Description:	(RW) This configuration option provides a way to force the device into remaining in
		the MAX power state.
		If idle support is enabled the device will transition to the `MIN` power state when
		idle, otherwise will stay in the MAX power state.
		Write to the file to enable or disable idle support.

		The values are:

		* 0: idle support is disabled
		* 1: idle support is enabled

		Default value is 1.

		It is possible to set the pm_idle_enabled value only if the device
		is in the `down` state (see /sys/bus/pci/devices/<BDF>/qat/state)

		The following example shows how to change the pm_idle_enabled of
		a device::

			# cat /sys/bus/pci/devices/<BDF>/qat/state
			up
			# cat /sys/bus/pci/devices/<BDF>/qat/pm_idle_enabled
			1
			# echo down > /sys/bus/pci/devices/<BDF>/qat/state
			# echo 0 > /sys/bus/pci/devices/<BDF>/qat/pm_idle_enabled
			# echo up > /sys/bus/pci/devices/<BDF>/qat/state
			# cat /sys/bus/pci/devices/<BDF>/qat/pm_idle_enabled
			0

		This attribute is only available for qat_4xxx devices.

What:		/sys/bus/pci/devices/<BDF>/qat/rp2srv
Date:		January 2024
KernelVersion:	6.7
Contact:	qat-linux@intel.com
Description:
		(RW) This attribute provides a way for a user to query a
		specific ring pair for the type of service that it is currently
		configured for.

		When written to, the value is cached and used to perform the
		read operation. Allowed values are in the range 0 to N-1, where
		N is the max number of ring pairs supported by a device. This
		can be queried using the attribute qat/num_rps.

		A read returns the service associated to the ring pair queried.

		The values are:

		* dc: the ring pair is configured for running compression services
		* sym: the ring pair is configured for running symmetric crypto
		  services
		* asym: the ring pair is configured for running asymmetric crypto
		  services

		Example usage::

			# echo 1 > /sys/bus/pci/devices/<BDF>/qat/rp2srv
			# cat /sys/bus/pci/devices/<BDF>/qat/rp2srv
			sym

		This attribute is only available for qat_4xxx devices.

What:		/sys/bus/pci/devices/<BDF>/qat/num_rps
Date:		January 2024
KernelVersion:	6.7
Contact:	qat-linux@intel.com
Description:
		(RO) Returns the number of ring pairs that a single device has.

		Example usage::

			# cat /sys/bus/pci/devices/<BDF>/qat/num_rps
			64

		This attribute is only available for qat_4xxx devices.

What:		/sys/bus/pci/devices/<BDF>/qat/auto_reset
Date:		May 2024
KernelVersion:	6.9
Contact:	qat-linux@intel.com
Description:	(RW) Reports the current state of the autoreset feature
		for a QAT device

		Write to the attribute to enable or disable device auto reset.

		Device auto reset is disabled by default.

		The values are:

		* 1/Yy/on: auto reset enabled. If the device encounters an
		  unrecoverable error, it will be reset automatically.
		* 0/Nn/off: auto reset disabled. If the device encounters an
		  unrecoverable error, it will not be reset.

		This attribute is only available for qat_4xxx devices.

What:		/sys/bus/pci/devices/<BDF>/qat/num_rps_per_vf
Date:		May 2025
KernelVersion:	6.15
Contact:	qat-linux@intel.com
Description:
		(RO) Returns the number of ring pairs per VF for the device.

		Example usage::

			# cat /sys/bus/pci/devices/<BDF>/qat/num_rps_per_vf
			4

		This attribute is only available for qat_4xxx and qat_6xxx devices.

What:		/sys/bus/pci/devices/<BDF>/qat/uacce
Date:		TBD
KernelVersion:	TBD
Contact:	qat-linux@intel.com
Description:	(RW) Read reports the current state of the uacce framework
		enablement. Write to the attribute enables or disables
		QAT utilization of the uacce framework.

		The uacce framework enablement allows QAT devices to incorporate
		the use of Intel's technology called Shared Virtual Memory (SVM),
		which provides the capability for user space data buffers to be
		used directly by the accelerator hardware with the virtual
		address instead of the physical address.

		Uacce framework is disabled by default.

		The allowed values are:

		* 1/Yy/on: Enable uacce framework.
		* 0/Nn/off: Disable uacce framework.

		It is possible to set this attribute only if the device
		is in the `down` state (see /sys/bus/pci/devices/<BDF>/qat/state)

		The following example shows how to switch QAT driver to use
		uacce framework::

			# cat /sys/bus/pci/devices/<BDF>/qat/state
			up
			# cat /sys/bus/pci/devices/<BDF>/qat/uacce
			off
			# echo down > /sys/bus/pci/devices/<BDF>/qat/state
			# echo 1 > /sys/bus/pci/devices/<BDF>/qat/uacce
			# echo up > /sys/bus/pci/devices/<BDF>/qat/state
			# cat /sys/bus/pci/devices/<BDF>/qat/uacce
			on

		This attribute is only available for qat_4xxx and
		qat_6xxx devices.

What:		/sys/bus/pci/devices/<BDF>/qat/ring_queue_mode
Date:		TBD
KernelVersion:	TBD
Contact:	qat-linux@intel.com
Description:	(RW) Read reports the current QAT device queue mode

		Write to the attribute to set the queue mode.

		The values are:

		* "wq": Worker Queue, rings pairs are dedicated to a single
		  process
		* "uq": User Queue, ring pairs can be shared by multiple
		  processes

		This attribute is only taken into account if the device is
		working with uacce framework enabled (see
		/sys/bus/pci/devices/<BDF>/qat/uacce)

		It is possible to set the configuration only if the device
		is in the `down` state (see /sys/bus/pci/devices/<BDF>/qat/state)

		The following example shows how to switch QAT driver into
		worker queue mode::

			# cat /sys/bus/pci/devices/<BDF>/qat/state
			up
			# cat /sys/bus/pci/devices/<BDF>/qat/uacce
			on
			# cat /sys/bus/pci/devices/<BDF>/qat/ring_queue_mode
			uq
			# echo down > /sys/bus/pci/devices/<BDF>/qat/state
			# echo wq > /sys/bus/pci/devices/<BDF>/qat/ring_queue_mode
			# echo up > /sys/bus/pci/devices/<BDF>/qat/state
			# cat /sys/bus/pci/devices/<BDF>/qat/ring_queue_mode
			wq

		This attribute is only available for qat_4xxx and
		qat_6xxx devices.

What:		/sys/bus/pci/devices/<BDF>/qat/caps_cksum_algos
Date:		TBD
KernelVersion:	TBD
Contact:	qat-linux@intel.com
Description:	(RO) Reports the checksum algorithms capabilities supported by
		the device.

		The value is a 16-bit bit mask and is displayed in hex.
		Each bit of the mask reports whether the capability is supported
		(1) or not (0) according to following mapping:

		* bit 0: crc32
		* bit 1: adler/xxhash
		* bit 2: programmable 64 bit input/output crc
		* bit 3: crc64

		Reserved bits are omitted.

		This attribute is only available for qat_4xxx and
		qat_6xxx devices.

What:		/sys/bus/pci/devices/<BDF>/qat/caps_comp_algos
Date:		TBD
KernelVersion:	TBD
Contact:	qat-linux@intel.com
Description:	(RO) Reports the compression algorithms capabilities supported
		by the device.

		The value is a 16-bit bit mask and is displayed in hex.
		Each bit of the mask reports whether the capability is supported
		(1) or not (0) according to following mapping:

		* bit 0: deflate
		* bit 2: lz4
		* bit 3: lz4s
		* bit 6: zstd

		Reserved bits are omitted.

		This attribute is only available for qat_4xxx and
		qat_6xxx devices.

What:		/sys/bus/pci/devices/<BDF>/qat/caps_deflate
Date:		TBD
KernelVersion:	TBD
Contact:	qat-linux@intel.com
Description:	(RO) Reports the deflate capabilities supported by the device.

		The value is a 32-bit bit mask and is displayed in hex.
		Each bit of the mask reports whether the capability is supported
		(1) or not (0) according to following mapping:

		* bit 0: compression
		* bit 1: decompression
		* bit 3: stateless
		* bit 8: dynamic huffman
		* bit 10: dynamic huffman buffer request
		* bit 11: auto select best huffman tree
		* bit 12: end of last block
		* bit 13: uncompressed dictionary

		Reserved bits are omitted.

		This attribute is only available for qat_4xxx and
		qat_6xxx devices.

What:		/sys/bus/pci/devices/<BDF>/qat/caps_dev
Date:		TBD
KernelVersion:	TBD
Contact:	qat-linux@intel.com
Description:	(RO) Reports the capabilities supported by the device.

		The value is a 32-bit bit mask and is displayed in hex.
		Each bit of the mask reports whether the capability is supported
		(1) or not (0) according to following mapping:

		* bit 0: symmetric crypto
		* bit 1: asymmetric crypto
		* bit 2: cipher
		* bit 3: authentication
		* bit 5: compression
		* bit 8: zuc
		* bit 9: sha3
		* bit 12: hdkf
		* bit 13: ecedmont
		* bit 14: ext alg chain
		* bit 15: sha3 ext
		* bit 16: aes-gcm spc
		* bit 17: chacha poly
		* bit 18: sm2
		* bit 19: sm3
		* bit 20: sm4
		* bit 22: cnv integrity
		* bit 23: cnv integrity 64
		* bit 24: lz4 compression
		* bit 25: lz4s compression
		* bit 26: aes v2
		* bit 27: kpt
		* bit 29: zuc 256
		* bit 30: wireless crypto ext

		Reserved bits are omitted.

		This attribute is only available for qat_4xxx and
		qat_6xxx devices.

What:		/sys/bus/pci/devices/<BDF>/qat/caps_dev_dc_ext
Date:		TBD
KernelVersion:	TBD
Contact:	qat-linux@intel.com
Description:	(RO) Reports the extended compression capabilities supported by
		the device.

		The value is a 32-bit bit mask and is displayed in hex.
		Each bit of the mask reports whether the capability is supported
		(1) or not (0) according to following mapping:

		* bit 0: compress and verify
		* bit 8: compress and verify and recover
		* bit 9: e2e integrity crc
		* bit 10: append crc 64
		* bit 11: partial decompress
		* bit 12: zero padding
		* bit 16: dc chain compress + hash
		* bit 17: dc chain compress + encrypt
		* bit 18: dc chain compress + (hash, encrypt)
		* bit 19: dc chain compress + (encrypt, hash)
		* bit 20: dc chain compress + aead
		* bit 21: dc chain compress + aead
		* bit 21: dc chain hash + compress
		* bit 22: dc chain hash verify + decompress
		* bit 23: dc chain decrypt + decompress
		* bit 24: dc chain (hash, verify, decrypt) + decompress
		* bit 25: dc chain (decrypt, hash, verify) + decompress
		* bit 26: dc chain aead + decompress
		* bit 27: dc chain decompress + hash verify
		* bit 28: dc chain compress + aead + hash
		* bit 29: lz4 compress programmable crc over header + block data
		* bit 30: lz4 compress programmable crc over lz4 block data

		Reserved bits are omitted.

		This attribute is only available for qat_4xxx and
		qat_6xxx devices.

What:		/sys/bus/pci/devices/<BDF>/qat/caps_lz4
Date:		TBD
KernelVersion:	TBD
Contact:	qat-linux@intel.com
Description:	(RO) Reports the LZ4 capabilities supported by the device.

		The value is a 16-bit bit mask and is displayed in hex.
		Each bit of the mask reports whether the capability is supported
		(1) or not (0) according to following mapping:

		* bit 0: compression
		* bit 1: decompression
		* bit 3: stateless
		* bit 4: stateful-lite

		Reserved bits are omitted.

		This attribute is only available for qat_4xxx and
		qat_6xxx devices.

What:		/sys/bus/pci/devices/<BDF>/qat/caps_lz4s
Date:		TBD
KernelVersion:	TBD
Contact:	qat-linux@intel.com
Description:	(RO) Reports the LZ4S capabilities supported by the device.

		The value is a 16-bit bit mask and is displayed in hex.
		Each bit of the mask reports whether the capability is supported
		(1) or not (0) according to following mapping:

		* bit 0: compression
		* bit 1: decompression
		* bit 3: stateless
		* bit 4: stateful-lite
		* bit 5: uncompressed dictionary

		Reserved bits are omitted.

		This attribute is only available for qat_4xxx and
		qat_6xxx devices.

What:		/sys/bus/pci/devices/<BDF>/qat/caps_version
Date:		TBD
KernelVersion:	TBD
Contact:	qat-linux@intel.com
Description:	(RO) Reports version of the capabilities in the format
		<major>.<minor>.

What:		/sys/bus/pci/devices/<BDF>/qat/caps_zstd
Date:		TBD
KernelVersion:	TBD
Contact:	qat-linux@intel.com
Description:	(RO) Reports the ZSTD capabilities supported by the device.

		The value is a 16-bit bit mask and is displayed in hex.
		Each bit of the mask reports whether the capability is supported
		(1) or not (0) according to following mapping:

		* bit 0: compression
		* bit 1: decompression
		* bit 2: uncompressed dictionary
		* bit 3: dictionary id

		Reserved bits are omitted.

		This attribute is only available for qat_4xxx and
		qat_6xxx devices.
